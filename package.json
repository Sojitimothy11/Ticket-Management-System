{
  "name": "ticket-management-system",
  "private": true,
  "workspaces": ["client", "server", "e2e"],
  "scripts": {
    "build": "cd client && bun run build && cd ../server && bun run generate",
    "start": "cd server && bun run migrate:deploy && bun run start"
  }
}
